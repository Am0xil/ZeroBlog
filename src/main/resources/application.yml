# 服务器端口配置
server:
  port: 8088

# 数据库连接
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/blog?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver # 如果你的MySQL版本比较老，请使用 com.mysql.jdbc.Driver
    username: root
    password: 本地MySQL的密码
